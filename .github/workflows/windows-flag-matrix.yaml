name: Windows flag matrix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  windows-install:
    runs-on: windows-latest

    strategy:
      fail-fast: false
      matrix:
        include:
          - name: minimal
            enable_link_flags: "0"
            enable_cpu_flags: "0"
            enable_bigobj: "0"
          - name: link-only
            enable_link_flags: "1"
            enable_cpu_flags: "0"
            enable_bigobj: "0"
          - name: cpu-only
            enable_link_flags: "0"
            enable_cpu_flags: "1"
            enable_bigobj: "0"
          - name: bigobj-only
            enable_link_flags: "0"
            enable_cpu_flags: "0"
            enable_bigobj: "1"
          - name: full
            enable_link_flags: "1"
            enable_cpu_flags: "1"
            enable_bigobj: "1"

    env:
      R_REMOTES_NO_ERRORS_FROM_WARNINGS: true
      ITKR_ENABLE_WINDOWS_LINK_FLAGS: ${{ matrix.enable_link_flags }}
      ITKR_ENABLE_WINDOWS_CPU_FLAGS: ${{ matrix.enable_cpu_flags }}
      ITKR_ENABLE_WINDOWS_BIGOBJ: ${{ matrix.enable_bigobj }}

    name: ${{ matrix.name }}

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: release
          windows-path-include-rtools: true
          windows-path-include-mingw: true

      - uses: r-lib/actions/setup-pandoc@v2

      - name: Install dependencies
        shell: Rscript {0}
        run: |
          install.packages("remotes")
          remotes::install_deps(dependencies = TRUE)

      - name: Install ITKR
        shell: cmd
        run: |
          R CMD INSTALL --preclean . > install.log 2>&1
          if errorlevel 1 (
            echo ---- install.log tail ----
            powershell -NoLogo -NoProfile -Command "Get-Content install.log -Tail 200"
            exit /b 1
          )
